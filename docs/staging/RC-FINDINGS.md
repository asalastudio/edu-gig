# Candidate findings and pending decisions

- **Legacy draft privacy — locally repaired, live proof pending.** Unmanaged legacy drafts previously passed old/new agreement-list and old history visibility checks, including text-only drafts excluded from content migration. The shared predicate now requires uploading-party access for a draft without sharing, preserving district teammates and legitimate legacy non-draft visibility. Source finding and in-memory regressions are not a claim that a live data leak was observed.
- **Stable data versus mutable automation — implemented locally.** Two exact maps preserve stable default and require explicit automation selection. Stable can receive supplemental reviewer examples idempotently; fault and journey/reset commands are automation-only. Independent review and hosted identity proof remain pending.
- **Revision proof — implemented locally.** Build-generated source commit is compiled into both backend and staging HTML. Seed/reset reject missing/unbuilt/mismatched expected commits. Provider metadata and actual rendered/backend values must still be reconciled live; a deploy log or mutable label alone is insufficient.
- **Fixture completeness — implemented locally.** A separate manifest contains true explicit version parents and a no-agreement engagement. No legacy card grouping, reviewer record overwrite, real signature or synthetic credential-review evidence is asserted. Failed delivery fixtures are induced states, not observed provider failures.
- **Live verification — Blocked.** Full journey, migration, HTTP transport, real sessions, reset/watchdog capture, deployment binding, remote CI and independent whole-branch review are controller gates. Existing baseline or env-free public tests cannot substitute.
- **Unavailable checks.** Physical device browsers, screen-reader speech and real email delivery are Blocked. Cloudflare custom hostname is pending access. Provider callback behavior is unconfigured and not claimed.
- **Product decisions remain open.** Service-area policy, retention period, support SLA, background-check launch and fees are not established by this implementation. Signing/payment remain off-platform. No invented business outcomes or production release claim.
