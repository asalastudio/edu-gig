{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "buildCommand": "if [ \"$VERCEL_ENV\" = \"production\" ]; then npx convex deploy --cmd 'npm run build'; else (npx convex codegen --typecheck=disable || true) && npm run build; fi"
}
